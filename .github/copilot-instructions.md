# Copilot Instructions

## General Guidelines
- Never stop/kill the running Quant.exe process just to do a compile. Let the build fail with LNK1168 instead of force-stopping the process.
- Show console/log output in an on-page console panel within the app UI, not in the browser's developer tools console.